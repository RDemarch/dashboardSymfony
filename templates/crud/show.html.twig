{% extends 'base.html.twig' %}

{% block title %}Crud{% endblock %}

{% block body %}
    <h1>Crud</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ crud.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ crud.name }}</td>
            </tr>
            <tr>
                <th>Credential</th>
                <td>{{ crud.credential }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ crud.category}}</td>
            </tr>
            <tr>
                <th>Buying Date</th>
                <td>{{ crud.buyingDateToString() }}</td>
            </tr>
            <tr>
                <th>End Waranty</th>
                <td>{{ crud.endWarantyDateToString() }}</td>
            </tr>
            <tr>
                <th>Maintaining Advice</th>
                <td>{{ crud.maintenanceAdvice }}</td>
            </tr>
            <tr>
                <th>User Manual</th>
                <td>{{ crud.userManual }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ crud.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('crud_index') }}">back to list</a>

    <a href="{{ path('crud_edit', {'id': crud.id}) }}">edit</a>

    {{ include('crud/_delete_form.html.twig') }}
{% endblock %}
